const instance = require('./instance')
const auth = require('./auth')
const credidentalProvider = require('./credidental_provider')

module.exports = {
    instance,
    auth,
    credidentalProvider
}